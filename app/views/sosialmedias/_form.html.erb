<%= simple_form_for(@sosialmedia) do |f| %>
  <div class="panel-body">
    <div class="row">
      <div class="col-md-12">
        <%= render 'form/error', form: @sosialmedia %>
      </div>
    </div>
    <div class="row">
      <div class="col-sm-12">
        <div class="row">
          <%= render('form/input', form: f, width: 12, caption: 'Tanggal', name: :tanggal, type: 'date'); %>
          <%= render('form/input', form: f, width: 12, caption: 'Link', name: :link); %>
          <%= render('form/input', form: f, width: 12, caption: 'Bencana', name: :bencana, type: 'select', select: :nama, value: :bencana); %>
        </div>
      </div>
    </div>
  </div>
  <div class="panel-footer text-right">
    <button type="submit" class="btn btn-default">Submit</button>
  </div>
<% end %>
