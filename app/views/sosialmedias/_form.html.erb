<%= simple_form_for(@sosialmedia) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :tanggal %>
    <%= f.input :link %>
    <%= f.association :bencana %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
