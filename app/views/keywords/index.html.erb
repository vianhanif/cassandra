<div class="container">
  <div class="row">
    <div class="col-md-12">
      <p id="notice"><%= notice %></p>
      <div class="panel panel-default panel-admin" style="margin-top:100px;">
        <div class="panel-heading">
          Keywords
          <%= link_to 'New Keyword', new_keyword_path, class: 'pull-right' %>
        </div>
        <div class="panel-body table-responsive">
          <%= form_tag import_keywords_path, multipart: true do %>
          <ul class="list-inline">
            <li>  <%= file_field_tag :file %></li>
            <li style="transform: translateX(-90px);"><%= submit_tag "Import Data", class: 'btn btn-default'%></li>
          </ul>
          <% end %>
          <table class="table table-hover">
            <thead>
              <tr>
                <th>Nama</th>
                <th>Bencana</th>
                <th colspan="3"></th>
              </tr>
            </thead>

            <tbody>
              <% @keywords.each do |keyword| %>
                <tr>
                  <td><%= keyword.nama %></td>
                  <td><%= keyword.bencana %></td>
                  <td><%= link_to 'Show', keyword %></td>
                  <td><%= link_to 'Edit', edit_keyword_path(keyword) %></td>
                  <td><%= link_to 'Destroy', keyword, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
      <div class="row">
        <div class="col-md-12">
          <%= will_paginate @keywords %>
        </div>
      </div>
    </div>
  </div>
</div>
