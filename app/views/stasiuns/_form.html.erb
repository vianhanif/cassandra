<%= simple_form_for(@stasiun) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nama %>
    <%= f.association :provinsi %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
