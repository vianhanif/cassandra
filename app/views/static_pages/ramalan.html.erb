<div class="container-fluid">
  <div class="home-page">
    <div class="row">
      <div class="col-sm-4">
        <div class="panel panel-default">
          <div class="panel-body">
            <%= form_tag('/ramalan', :method => "get", role: "search") do %>
              <div class="form-inline">
                <div class="form-group">
                  <label for="exampleInputName2">Provinsi</label>
                  <%= collection_select :provinsi, :id, Provinsi.all, :id, :nama, {:include_blank => 'Pilih...'}, {:class => 'form-control form-select'} %>
                </div>
                <div class="form-group">
                  <label for="exampleInputEmail2">Bulan</label>
                  <%= collection_select :bulan, :id, [
                    'Januari',
                    'Februari',
                    'Maret',
                    'April',
                    'Mei',
                    'Juni',
                    'Juli',
                    'Agustus',
                    'September',
                    'Oktober',
                    'November',
                    'Desember',
                  ],
                    :to_s, :to_s, {:include_blank => 'Pilih...'}, {:class => 'form-control form-select'} %>
                </div>
                <button type="submit" class="btn btn-default">Lihat Hasil</button>
            </div>
            <% end %>
          </div>
        </div>
      </div>
      <% if params[:provinsi] and params[:bulan] %>
        <div class="col-sm-12">
          <div class="panel panel-default">
            <div class="panel-body">
              <h3 style="color:#000000; margin-bottom: 20px;">Berdasarkan Data yang ada, di <b><%= Provinsi.find(params[:provinsi][:id]).nama %></b> pada bulan <b><%= params[:bulan][:id] %></b>, kemungkinan bencana yang terjadi adalah :  </h3>
              <div class="text-center">
                <div class="row">
                  <div class="col-xs-8 col-xs-offset-2">
                    <ul class="list-inline">
                      <li><%= fa_icon "exclamation-triangle", class: 'img-ramalan' %></li>
                      <li class="ramalan-text-main"><span class="big-text"> <%= ramal(params[:provinsi][:id], set_bulan(params[:bulan][:id])).nama.downcase.capitalize %> </span></li>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
